let nome, idade, quantidade;

quantidade = Number(prompt(`Quantas vezes? `))

    for(i=0; i<quantidade; i++){

        nome = prompt(`Digite um nome: ${i+1}`);
        idade = prompt(`Digite uma idade: ${i+1}`);

    }

/*
let nome, idade;

    for(i=50; i>0; i--){

        nome = prompt(`Digite um nome: `);
        idade = prompt(`Digite uma idade: `);

    }
*/
/*
let cont = 0;

    while(cont < 5){

        nome = prompt(`Digite um nome: `);
        idade = prompt(`Digite uma idade: `);
        cont++

    }
*/
