QUESTÃO 1:
SELECT 
	QUIZ.IDQUIZ
	, QUIZ.TITULO
	, PERGUNTA.IDPERGUNTA
	, PERGUNTA,ENUCIADO
	, OPCAO.IDOPCAO
	, OPCAO.TEXTO
	, OPCAO.CERTA
FROM
	QUIZ
	INNER JOIN PERGUNTA ON
	PERGUNTA.IDQUIZ
	INNER JOIN OPCAO ON
	OPCAO.IDPERGUNTA = PERGUNTA.IDPERGUNTA
WHERE
	QUIZ.IDQUIZ = 2;


QUESTÃO 2:

SELECT 
	PARTICIPANTE.IDPARCIPANTE
	, PARTIPANTE.NOME
	, PARTICIPANTE.EMAIL
	, RESPOSTA.OPCAO_IDOPCAO
	, QUIZ.TITULO
	, PERGUNTA.ENUNCIADO
	, OPCAO.TEXTO
FROM
	PARTICIPANTE
	INNER JOIN RESPOSTA ON
	PARTICIPANTE.IDPARTICIPANTE = REPOSTA.IDPARTICIPANTE
	INNER JOIN PERGUNTA ON
	RESPOSTA.IDPERGUNTA = PERGUNTA.IDPERGUNTA
	INNER JOIN QUIZ ON
	QUIZ.IDQUIZ = PERGUNTA.IDQUIZ
	INNER JOIN OPCAO ON
	OPCAO.IDOPCAO = RESPOSTA.OPCAO_IDOPCAO;


QUESTÃO 3:
SELECT 
	PERGUNTA.IDPERGUNTA
	, PERGUNTA.ENUNCIADO
	, OPCAO.IDOPCAO
	, OPCAO.TEXTO
FROM
	QUIZ
	INNER JOIN PERGUNTA ON
	PERGUNTA.IDQUIZ
	INNER JOIN OPCAO ON
	OPCAO.IDPERGUNTA = PERGUNTA.IDPERGUNTA
WHERE
	QUIZ.TIPO = 'T'
	AND OPCAO.CERTA = 'S';


QUESTAO 4:

SELECT 
	IDPERGUNTA
	, COUNT(IDPARTICIPANTE)
FROM
	RESPOSTA
GROUP BY
	IDPERGUNTA;

QUESTÃO 5:
SELECT
	MATRICULA.IDMATRICULA
	, DISCIPLINA.IDDISCIPLINA
	, DISCIPLINA.NOME
	, ALUNO.IDALUNO
	, ALUNO.NOME
	, MATRICULA.ANO
	, MATRICULA.SEMESTRE
	, MATRICULA.SITUACAO
FROM
	MATRICULA
	INNER JOIN DISCIPLINA ON
	DISCIPLINA.IDDISCIPLINA = MATRICULA.IDDISCIPLINA
	INNER JOIN ALUNO ON
	ALUNO.IDALUNO = MATRICULA.IDDALUNO
WHERE
	MATRICULA.ANO = 2015
	AND MATRICULA.SEMESTRE = 1;

QUESTÃO 6:
SELECT
	MATRICULA.IDMATRICULA
	, DISCIPLINA.IDDISCIPLINA
	, DISCIPLINA.NOME
	, ALUNO.IDALUNO
	, ALUNO.NOME
	, MATRICULA.ANO
	, MATRICULA.SEMESTRE
	, MATRICULA.SITUACAO
FROM
	MATRICULA
	INNER JOIN DISCIPLINA ON
	DISCIPLINA.IDDISCIPLINA = MATRICULA.IDDISCIPLINA
	INNER JOIN ALUNO ON
	ALUNO.IDALUNO = MATRICULA.IDDALUNO
WHERE
	MATRICULA.ANO = 2015
	AND MATRICULA.SEMESTRE = 2
	AND DISCIPLINA.NOME LIKE '%ANTROPOLOGIA TEOLÓGICA%'
ORDER BY ALUNO.NOME;

QUESTÃO 7:
SELECT 
	MATRICULA.ANO
	, MATRICULA.SEMESTRE
	, MATRICULA.SITUACAO
	, COUNT(MATRICULA.IDMATRICULA)
	, COUNT(MATRICULA.IDALUNO)
	, COUNT(MATRICULA.IDDISCIPLINA)
FROM
	MATRICULA
GROUP BY
	MATRICULA.ANO
	, MATRICULA.SEMESTRE
	, MATRICULA.SITUACAO



